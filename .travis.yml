language: php

script:
#  - echo `which jq`
#  - jq --version
#  - composer require `cat ./Plugin/composer.json | jq -r '.name'`:@dev --dev

  - npm install -g bower
  - echo `which bower`
  - bower --version
#  - bower install
